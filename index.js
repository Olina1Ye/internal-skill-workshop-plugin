/**
 * Host half for the DSH bundle. The first release is a browser-only catalog
 * view, so the host half deliberately has no filesystem or network behavior.
 */
export const name = 'internal-skill-workshop';

export function apply() {}
